SERVER_ADDRESS="{{ .ServerAddress }}"
POSTGRES_HOST="{{ .DbHost }}"
POSTGRES_PORT="{{ .DbPort }}"
POSTGRES_DB="{{ .DbName }}"
POSTGRES_USER="{{ .DbUser }}"
POSTGRES_PASSWORD="{{ .DbPassword }}"
TOKEN_ISSUER="{{ .AppName }}"
DEBUG="{{ .Debug }}"

SCHEDULER_SERVER_ADDRESS="{{ .SchedulerServerAddress }}"
SCHEDULER_POSTGRES_HOST="{{ .SchedulerDbHost }}"
SCHEDULER_POSTGRES_PORT="{{ .SchedulerDbPort }}"
SCHEDULER_POSTGRES_DB="{{ .SchedulerDbName }}"
SCHEDULER_POSTGRES_USER="{{ .SchedulerDbUser }}"
SCHEDULER_POSTGRES_PASSWORD="{{ .SchedulerDbPassword }}"
SCHEDULER_SECRET="{{ .SchedulerSecret }}"
SCHEDULER_API_KEY="{{ .SchedulerApiKey }}"
SCHEDULER_DEBUG="{{ .SchedulerDebug }}"
