output   = "models"
wipe     = true
no-tests = true
add-enum-types = true

[psql]
  dbname = "todo"
  host   = "localhost"
  port   = 5432
  user   = "postgres"
  pass   = "secret"
  sslmode = "disable"
  blacklist = ["schema_version", "casbin_rule"]
